# Checklist

## 安全与可靠性
- [x] `backend/.env.example` 不含真实 Kimi 密钥（为占位符）
- [x] `docs/项目交接文档.md` 不含真实 Kimi 密钥
- [x] `backend/app/main.py` 已 `import io`，上传 DOCX 不再抛 NameError
- [x] 流式匹配在 LLM 重试仍失败时返回可渲染的降级结果（result 事件），前端不白屏
- [x] `main.py` `__main__` 端口与实际部署一致（8888）

## 前端功能
- [x] "沉淀为经历原子"在分析完成后可成功创建原子
- [x] "一键优化"与"沉淀原子"按钮点击仅触发一次（无重复请求/重复入库）
- [x] 429/限流场景前端显示友好中文提示，不透传英文报错

## 岗位推荐
- [x] 存在 `backend/data/job_pool.json` 岗位池种子数据
- [x] `POST /api/v1/jobs/recommend` 返回按匹配度降序的 Top-N 岗位（含分数与理由）
- [x] 前端「岗位推荐」标签页可展示推荐岗位并支持"去分析"带 JD 进工作台
- [x] 痛点1→痛点2闭环可演示：推荐岗位 → 进入匹配工作台 → 招聘官视角分析

## 整体
- [x] 前后端可正常启动，核心匹配主链路与新增推荐功能均跑通
